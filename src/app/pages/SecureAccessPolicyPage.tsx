import { SecureAccessPolicy } from '../components/SecureAccessPolicy';

export function SecureAccessPolicyPage() {
  return (
    <div className="min-h-screen bg-[#fafbfc]">
      <SecureAccessPolicy />
    </div>
  );
}
